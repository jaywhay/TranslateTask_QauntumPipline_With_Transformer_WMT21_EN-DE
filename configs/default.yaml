data:
  raw_path: D:\QnnCD\NLP\data\WMT14.csv
  cache_path: D:\QnnCD\NLP\cache\wmt14_parsed.pkl
training:
  batch_size: 32
  max_length: 64
  seed: 42
model:
  embed_dim: 64
  head_dim: 32
  num_heads: 4
  num_quantum_heads: 1
  qubits: 6
  layers: 3
optimizer:
  lr: 1e-4
  weight_decay: 0.01
scheduler:
  type: CosineAnnealingLR
  T_max: 10
